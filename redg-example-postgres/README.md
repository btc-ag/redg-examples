# RedG with PostgreSQL

This example demonstrates the usage of RedG with a PostgreSQL database.

# Prerequisites

Create a database named `redg` and run the content of `schema.sql` when connected to that
database.